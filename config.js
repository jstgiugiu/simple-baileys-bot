import { fileURLToPath } from 'url';
import path from 'path';

global.owners = ['17789072704', '393445461546']; // Inserisci i numeri senza + e senza @s.whatsapp.net
global.defaultLang = 'en'; 

export default global;